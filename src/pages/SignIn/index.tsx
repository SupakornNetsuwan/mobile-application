import SignInProvider from "./providers/SignInProvider";
import Page from "./Page";

const SignIn = () => {
  
  return (
    <SignInProvider>
      <Page />
    </SignInProvider>
  );
};

export default SignIn;
